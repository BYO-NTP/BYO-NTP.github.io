{
  "dependencies": {
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "start": "npm run server",
    "server": "npx nodemon --watch . --exec node server.js"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}

{
  "dependencies": {
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "all": "node tools/all-from-path.js",
    "start": "npm run server",
    "server": "npm run all && npx nodemon --watch . --exec node server.js"
  }
}